import type { BaseFormType, BaseType } from "../common/base_type";

export interface CrmVersion extends BaseType {
	name: string;
}

export interface Form_CrmVersion extends BaseFormType {
	name: string;
}
